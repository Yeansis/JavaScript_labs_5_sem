function main_func(f) { //принимает в качестве переменной функцию
    return function(a,b,c) { //возвращает замыкание, которое принимает массив аргументов "a,b,с"
        //??? сопоставление
    };
}

let func = function() {

}

main_func(func)

